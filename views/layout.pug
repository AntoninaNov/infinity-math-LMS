doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title NMT LMS
        link(href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet")
    body(class="flex flex-col min-h-screen")
        header.bg-blue-800.text-white
            .container.mx-auto.flex.justify-between.items-center.p-4
                h1.text-3xl.font-bold MATHPEAK
                nav
                    a(href="/" class="px-4 py-2 hover:underline") Home
                    if user
                        a(href="/profile" class="px-4 py-2 hover:underline") Profile
                        a(href="/logout" class="px-4 py-2 hover:underline") Logout
                    else
                        a(href="/login" class="px-4 py-2 hover:underline") Login
                        a(href="/register" class="px-4 py-2 hover:underline") Register
        main.container.mx-auto.p-4.flex-grow
            block content
        footer.bg-gray-800.text-white.text-center.p-4.mt-8
            p © 2024 Mathpeak LMS. All rights reserved.
