doctype html
html
    head
        title User List
    body
        h1 User List
        if users.length
            ul
                each user in users
                    li
                        a(href=`/users/${user._id}`)= user.name + ' - ' + user.email
        else
            p No users found.
